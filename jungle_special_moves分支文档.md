修改总结
1. 添加的函数（在 moves.c 中）
河流检查函数
IsRiverSquare(int row, int col) - 检查指定位置是否为河流
河流区域：行3-5，列1-2（左片）和列4-5（右片）
跳河相关函数
HasRatInRiverPath() - 检查跳河路径中是否有老鼠阻挡
GenerateRiverJump() - 生成狮子和老虎的跳河走法
2. 修改的走法生成函数
StepForward() (moves.c:644-651)
添加河流检查：除老鼠外，其他棋子不能进入河流
StepBackward() (moves.c:654-665)
添加河流检查：除老鼠外，其他棋子不能进入河流
StepSideways() (moves.c:753-785)
添加河流检查：除老鼠外，其他棋子不能进入河流
3. 集成跳河逻辑
老虎（WhiteRook/BlackRook）(moves.c:1328)
在 VariantJungle 中调用 GenerateRiverJump() 生成跳河走法
狮子（WhiteLion/BlackLion）(moves.c:1462)
在 VariantJungle 中调用 GenerateRiverJump() 生成跳河走法
4. 修复编译错误
添加了函数前向声明 (moves.c:639-642)
在使用这些函数之前声明，解决 "static declaration follows non-static declaration" 错误
实现的功能
河流限制：除老鼠外，其他棋子不能进入河流区域
老鼠入河：老鼠可以正常进入河流
狮虎跳河：狮子和老虎可以从河流边缘跳过整片河流
跳河阻挡：跳河路径中有老鼠（无论敌我）则不能跳
跳河落点：可以落在对岸的陆地格子（空格或可吃的敌方棋子）
代码已通过编译检查，可以测试这些功能。


代码逻辑主要有：
创建一个判断一个位置是否为河流的函数】
在原xboard的上下左右移动函数中增加对于河流的检查
只要位于河流且不是老鼠，走棋就不合法

同时：对于老虎和狮子，增加跳河的逻辑，也就是说新添加合法的走棋位置
这是由一个新增的函数实现的。


增加棋子大小关系判断逻辑，能够检查落点棋子的等级，判断能吃子

修改总结
1. 新增函数（在 moves.c 中）
IsTrapSquare(int row, int col, int side) (moves.c:712-725)
功能：检查指定位置是否为陷阱格子
逻辑：
白方陷阱：行1，列2-4（对方兽穴前方及左右方）
黑方陷阱：行7，列2-4（对方兽穴前方及左右方）
返回1表示是陷阱，0表示不是
GetPieceRank(ChessSquare piece) (moves.c:727-757)
功能：获取棋子等级
逻辑：
象(8) > 狮(7) > 虎(6) > 豹(5) > 狼(4) > 狗(3) > 猫(2) > 鼠(1)
根据棋子类型返回对应等级
非斗兽棋棋子返回0
CanCaptureJungle(...) (moves.c:759-810)
功能：检查是否可以吃子
逻辑：
水战限制：
陆地棋子不能吃水中棋子（除鼠外）
水中鼠不能吃岸上象
陷阱效果：
敌方陷阱中的棋子可被任何敌方棋子吃掉
特殊规则：
老鼠可以吃大象
大象不能吃老鼠
基本规则：
大吃小（包括同级，即 >=）
2. 修改的走法生成函数
StepForward() (moves.c:644-658)
修改：在调用callback前添加吃子检查
逻辑：目标格子有敌方棋子时，先调用CanCaptureJungle()检查，不能吃子则不生成该走法
StepBackward() (moves.c:660-674)
修改：在调用callback前添加吃子检查
逻辑：同StepForward()
StepSideways() (moves.c:758-800)
修改：在调用callback前添加吃子检查
逻辑：
左右两个方向分别检查
目标格子为空或可以吃子时才生成走法
GenerateRiverJump() (moves.c:812-860)
修改：在生成跳河走法时添加吃子检查
逻辑：跳河落点有敌方棋子时，检查是否可以吃子
3. 代码逻辑说明
吃子检查流程
检查目标格子是否为空
为空：允许移动
不为空：继续检查
水战限制检查
陆地棋子不能吃水中棋子（除鼠外）
水中鼠不能吃岸上象
陷阱效果检查
敌方陷阱中的棋子可被任何敌方棋子吃掉
特殊规则检查
老鼠可以吃大象
大象不能吃老鼠
基本大小关系检查
攻击方等级 >= 防守方等级
走法生成流程
检查边界和颜色
检查河流限制（非鼠不能入河）
检查吃子规则（目标有棋子时）
生成走法（调用callback）
4. 实现的规则
棋子大小关系：象(8) > 狮(7) > 虎(6) > 豹(5) > 狼(4) > 狗(3) > 猫(2) > 鼠(1)
同级互吃：同级可以互吃（>=）
大吃小：大棋可以吃小棋
象鼠反转：老鼠可以吃大象，但大象不能吃老鼠
陷阱效果：进入敌方陷阱的棋子可被任何敌方棋子吃掉
水战限制：陆地棋子不能吃水中棋子（除鼠外），水中鼠不能吃岸上象
5. 代码位置
陷阱检查函数：moves.c:712-725
棋子等级函数：moves.c:727-757
吃子检查函数：moves.c:759-810
走法生成修改：moves.c:644-658 (StepForward), moves.c:660-674 (StepBackward), moves.c:758-800 (StepSideways)
跳河吃子检查：moves.c:847-850 (GenerateRiverJump)
代码已通过编译检查，无语法错误。吃子规则已集成到走法生成中，确保只生成合法的吃子走法。